// v2 shell. The real design-system-driven UI is built in phases V4 (tokens +
// primitives) and V5 (the seven home sections), wired to the Hono API.
export function App() {
  return (
    <main style={{ "font-family": "system-ui, sans-serif", padding: "2rem" }}>
      <h1>Music Transfer — Self Serve</h1>
      <p>v2 shell. The UI is built in phases V4–V5.</p>
    </main>
  );
}
